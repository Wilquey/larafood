sudo service docker start

sudo service docker stop

sudo service docker status

docker-compose up -d

docker-compose ps

docker exec -it larafood_app_1 bash

git pull origin master --allow-unrelated-histories
